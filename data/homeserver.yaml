# Configuration file for Synapse.
#
# This is a YAML file: see [1] for a quick introduction. Note in particular
# that *indentation is important*: all the elements of a list or dictionary
# should have the same indentation.
#
# [1] https://docs.ansible.com/ansible/latest/reference_appendices/YAMLSyntax.html
#
# For more information on how to configure Synapse, including a complete accounting of
# each option, go to docs/usage/configuration/config_documentation.md or
# https://element-hq.github.io/synapse/latest/usage/configuration/config_documentation.html
server_name: "localhost"
pid_file: /data/homeserver.pid
listeners:
  - port: 8008
    resources:
    - compress: false
      names:
      - client
      - federation
    tls: false
    type: http
    x_forwarded: true
database:
  name: psycopg2
  args:
    user: synapse_user
    password: synapse_pass
    database: synapse
    host: localhost
    port: 5432
    cp_min: 5
    cp_max: 10
log_config: "/data/localhost.log.config"
media_store_path: /data/media_store
registration_shared_secret: "1j;9LkiUO8Y#KF+Szrb#fgflrVjIsMSgvAwR10*,-1j35^OPV^"
report_stats: false
macaroon_secret_key: "p7l&:sb&LsU^#S5H6ldU:O00DdniuSpP5h,tysV@^~N.XWW@;v"
form_secret: "9+rWa@K~Vj=mPX9P,#Lxs,~Nd43DCf*,K+1OzX&Km5bAQxWWG0"
signing_key_path: "/data/localhost.signing.key"
trusted_key_servers:
  - server_name: "matrix.org"
turn_uris:
  - "turn:192.168.103.24:3478?transport=udp"

turn_username: "test"
turn_password: "test123"

turn_allow_guests: true

# vim:ft=yaml